<!-- 这是自定义组件的内部WXML结构 -->
<navigator class="{{className}}" url="{{to}}">
  <view class="link-icon" wx:if='{{icon}}' style="background-image: {{'url(' + icon + ')'}}"></view>
  <view class="link-text">{{innerText}}</view>
</navigator>